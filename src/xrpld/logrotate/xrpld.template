${CONFIG_DIR}/log/*.log {
  daily
  minsize 200M
  rotate 7
  nocreate
  missingok
  notifempty
  compress
  compresscmd /usr/bin/nice
  compressoptions -n19 ionice -c3 gzip
  compressext .gz
  postrotate
    ${CONFIG_DIR}/bin/xrpld --conf ${CONFIG_FILE} logrotate
  endscript
}