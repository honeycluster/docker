## Codebase Patterns
- Code lives in `packages/generator/` (monorepo package)
- Tests use Vitest with `import { describe, it, expect } from 'vitest'`
- Test files are in `packages/generator/tests/` mirroring `src/` structure
- Run tests: `cd packages/generator && npx vitest run`
- Typecheck: `npx tsc --noEmit -p packages/generator/tsconfig.json` (from repo root, not package dir)
- Full generator pipeline: `resolveXrpldConfig(input)` → `validateXrpldConfig(resolved)` → `renderXrpldCfg(resolved)` → `generateValidatorsTxt(resolved)`
- Use `generateXrpldConfig(input)` for the all-in-one entry point returning `{ config, validatorsTxt, warnings }`
- `validateXrpldConfig(config: XrpldInput)` is in `validation.ts` — returns `{ errors, warnings }` with typed `ValidationEntry[]`
- CLI is at `packages/cli/`
- Preset defaults pattern: export `VALID_*` ReadonlySet + `get*Defaults()` function + `*_MAP` record
- Merge order in resolveXrpldConfig: common < size < role < verbosity < network < user
- When changing defaults, regenerate expected .cfg files
- Invalid preset values crash `resolveXrpldConfig` — test invalid presets by constructing config with spread, not via `makeConfig()`
- Validation enforces ledger_history <= online_delete — size presets must respect this constraint
- Default presets: network=mainnet, role=stock, size=medium, verbosity=warning
- The `examples` directory is gitignored at repo root — don't create example files there
- Always test presets through `generateXrpldConfig` (not just unit tests) to catch validation violations
- Pre-existing test failures: cli.test.ts, presets.test.ts (process.exit), and 2 tests on missing example .cfg (gitignored) — ignore these
- Must run `pnpm install` (or `CI=true pnpm install`) before typecheck to resolve monorepo deps
- `validator_list_key_sources` parallel array in VL config associates keys with domain comments in validators.txt

---

## Completed Stories

All user stories (US-001 through US-018) completed on 2026-03-19 to 2026-03-20.
- US-001–US-010: v2 rewrite (typed interfaces, section-based rendering, validation, parsers, integration tests)
- US-011–US-018: Node role presets (roles, sizes, verbosity, CLI flags, preset validation, exports, integration tests)
- Coverage: 90%+ across 342 tests

---


# Ralph Progress Log
Started: Thu Mar 26 09:33:22 PM UTC 2026
---

## 2026-03-26 - Planning Phase
- Implementation plan created at scripts/ralph/plan.md
- 5 phases identified: VL removal → Ports & validator role → SSL certs → User overrides → Tests
- 8 user stories analyzed with dependency ordering and risk assessment
- Key architectural decisions:
  - Remove 'vl' from KEY_VALUE_KEYS to stop cfg rendering (data stays for validators-txt)
  - Add validator_list_key_sources parallel array for comment generation
  - Expand DEFAULT_PORTS from 4 to 6 with dual-protocol (http,https / ws,wss)
  - Add server_comment field to XrpldInput for validator warning
  - Use openssl via execFileSync for cert generation (Node.js crypto can't create X.509)
  - Update protocol validation to allow same-family dual protocols
- **Learnings for future iterations:**
  - deepMerge replaces arrays entirely — role overrides with ports must specify full port set
  - Node.js crypto.X509Certificate is read-only — cannot create certs, must use openssl
  - examples/ directory is gitignored — skip example file creation ACs
  - VL validation in validateProtocol() is cfg-specific — remove when removing VL from cfg
---

## 2026-03-26 - US-001: Remove VL stanzas from cfg output
- Removed `'vl'` from `KEY_VALUE_KEYS` array in `cfg-renderer.ts` — stops [vl] section from appearing in .cfg output
- Removed VL HTTPS validation warning from `validateProtocol()` in `validation.ts`
- VL data remains in network defaults (consumed by validators-txt.ts, not cfg renderer)
- Updated tests: cfg-renderer.test.ts, validation.test.ts, integration.test.ts
- Updated 6 .cfg fixture files to remove [vl] sections
- Files changed:
  - `packages/generator/src/renderers/cfg-renderer.ts`
  - `packages/generator/src/validation.ts`
  - `packages/generator/tests/cfg-renderer.test.ts`
  - `packages/generator/tests/validation.test.ts`
  - `packages/generator/tests/integration/integration.test.ts`
  - `packages/generator/tests/integration/expected/xrpld-{mainnet,testnet,devnet,default,custom-ports,advanced}.cfg`
- **Learnings for future iterations:**
  - Pre-existing test failures: cli.test.ts and presets.test.ts fail due to process.exit side effect; 2 tests fail on missing example .cfg file (gitignored). These are unrelated to this PRD.
  - Must run `pnpm install` before typecheck (monorepo deps needed for base tsconfig)
---

## 2026-03-26 - US-002: Update validators.txt with current VL sources
- Added `validator_list_key_sources` optional field to `XrpldVlConfig` type for comment generation
- Updated MAINNET_DEFAULTS: added `https://unl.xrplf.org` to sites, added XRPLF key, added key sources
- Updated `generateValidatorsTxt` to render `#domain` comment lines before each key when sources are provided
- Updated mainnet expected validators.txt fixture with both VL sources and commented keys
- Updated defaults.test.ts to expect both mainnet VL sites, keys, and key sources
- Testnet and devnet VL sources unchanged
- Files changed:
  - `packages/generator/src/types/xrpld-input.ts`
  - `packages/generator/src/defaults/xrpld.ts`
  - `packages/generator/src/generators/validators-txt.ts`
  - `packages/generator/tests/defaults.test.ts`
  - `packages/generator/tests/integration/expected/xrpld-mainnet-validators.txt`
- **Learnings for future iterations:**
  - `validator_list_key_sources` is a parallel array to `validator_list_keys` — indices must match
  - Comment format in validators.txt is `#domain` (no space after #)
  - Keys without a corresponding source entry render without a comment (backwards compatible)
---
